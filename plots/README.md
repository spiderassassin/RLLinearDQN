Each line of input CSV should be formatted as:

```
<number of layers>,<average timesteps over all seeds>,<average layers over all seeds>
```

All of this information can be obtained from the aggregate.log file.
